internal class Program
{
    private static void Main(string[] args)
    {
        string ok = "Hello"; // we can put string values but need that on quotation mark without that the value wont work.
        int var = 10; // we can put the integar full value in here. N.B: we can not put point value in here
        double var2 = 10.12; //we can put two values after point 
        float var3 = 1.1; // we can put just one number after point
        bool var4 = true; // we can set 0 or 1 value with true and false. true means 1 and false means 0

        Console.ReadLine(); // for holding the console for see the output
    }
}
